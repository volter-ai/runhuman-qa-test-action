{
  "name": "@runhuman/github-action",
  "version": "0.0.1",
  "description": "GitHub Action for human-powered QA testing",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "npm run compile && npm run bundle",
    "compile": "tsc",
    "bundle": "ncc build lib/main.js --source-map --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint src/**/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volter-ai/runhuman.git",
    "directory": "packages/github-action"
  },
  "keywords": [
    "github-action",
    "qa",
    "testing",
    "human-in-the-loop",
    "ci-cd",
    "quality-assurance"
  ],
  "author": "Runhuman <runhuman@volter.ai>",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3",
    "vitest": "^3.2.4"
  }
}
